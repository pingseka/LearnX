# 监控配置贡献说明

姓名：许正扬<br>
学号：2312190402<br>
日期：2026-06-01

## 我完成的工作

### 1. 日志配置

- [x] 实现结构化 JSON 日志
- [x] 输出日志级别、请求路径、状态码和响应时间
- [x] 异常日志保留错误信息和调用栈

### 2. 健康检查

- [x] 实现 `/health` 端点
- [x] 检查 SQLite 数据库连接
- [x] 数据库异常时返回 HTTP `503`

### 3. 指标收集

- [x] 请求计数
- [x] 平均响应时间
- [x] HTTP `500` 及以上错误率
- [x] 使用监控令牌保护 `/metrics`

## PR 链接

- 待提交后补充

## 遇到的问题和解决

1. 问题：原来的 `morgan('dev')` 是文本日志，不方便检索。<br>
   解决：新增统一 JSON logger 和请求日志中间件。

2. 问题：原来的 `/health` 只返回固定成功，数据库损坏也看不出来。<br>
   解决：健康检查中真实执行数据库认证，失败时明确返回 `503`。

## 心得体会

监控的作用是尽早暴露问题。例如页面打不开时，先访问 `/health` 就能
区分是整个服务异常，还是单个页面接口异常，再结合 JSON 日志定位原因。
