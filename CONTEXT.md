# 项目上下文

- 现在在做什么：完成验收前联调修复，覆盖作者主页、审核资料预览、真实下单收益、设置页头像/昵称/密码。
- 上次停在哪：本地 API 已验证管理员审核成功、买家购买后卖家收益增加、头像上传和修改密码可用；浏览器插件被策略拦截，只能用 HTTP/构建/测试验证。
- 近期关键决定及原因：演示数据用 `npm run seed:demo --prefix backend` 导入，上传文件本地在 `backend/uploads`、生产在 Docker `uploads` volume，避免把数据写死在页面里。
